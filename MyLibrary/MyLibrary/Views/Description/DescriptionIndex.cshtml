@model IEnumerable<MyLibrary.DBContext.Description>

@{
    ViewBag.Title = "Index";
}
<style type='text/css'>
    /*設定 數字靠右*/
    td:nth-child(2){text-align: right;}
</style>
<h2>Index</h2>
<h4>Description</h4>
<p>
    @Html.ActionLink("Create New", "DescriptionCreate")
</p>
<table class="table">
    <tr>
        <th></th>
        <th>
            @Html.DisplayNameFor(model => model.Book.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescriptionContent)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Edit", "DescriptionEdit", new { id=item.Id })
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Book.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescriptionContent)
        </td>

    </tr>
}

</table>
